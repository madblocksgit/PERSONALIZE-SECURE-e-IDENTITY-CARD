import React from "react";
import { Message } from "semantic-ui-react";

/** Decribes the view when no file is found for a given user */

export default () => {
  return <Message negative>No Files Found!!!</Message>;
};
